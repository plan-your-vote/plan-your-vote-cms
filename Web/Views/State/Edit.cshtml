@model PlanYourVoteLibrary2.State

@using  Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer

@{
    ViewData["Title"] = @Localizer["Change Election"];
}

<h1>@ViewData["Title"]</h1>

<div class="panel panel-default">
    <form asp-action="Edit">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="StateId" />
        <div class="form-group">
            <label asp-for="RunningElection" class="control-label"></label>
            <select asp-for="RunningElectionID" class="form-control" asp-items="ViewBag.Elections"></select>
            <span asp-validation-for="RunningElectionID" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="ManagedElection" class="control-label"></label>
            <select asp-for="ManagedElectionID" class="form-control" asp-items="ViewBag.Elections"></select>
            <span asp-validation-for="ManagedElectionID" class="text-danger"></span>
        </div>
        <div class="form-group">
            <input type="submit" value=@Localizer["Save"] class="btn btn-primary" />
        </div>
    </form>
</div>

<div>
    <a class="btn btn-primary" asp-action="Index">@Localizer["Back to List"]</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
