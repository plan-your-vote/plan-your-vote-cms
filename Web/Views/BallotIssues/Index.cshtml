@model IEnumerable<VotingModelLibrary.Models.BallotIssue>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BallotIssueTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BallotIssueTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BallotIssueId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BallotIssueId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BallotIssueId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
