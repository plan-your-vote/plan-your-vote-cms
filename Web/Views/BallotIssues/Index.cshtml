@model IEnumerable<VotingModelLibrary.Models.BallotIssue>
@using  Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer

@{
    ViewData["Title"] = @Localizer["Index"];
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create">@Localizer["Create New"]</a>
</p>
<table class="table panel panel-default">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BallotIssueTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th class="col-sm-2"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.BallotIssueTitle)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.BallotIssueId">@Localizer["Edit"]</a> |
                    <a asp-action="Details" asp-route-id="@item.BallotIssueId">@Localizer["Details"]</a> |
                    <a asp-action="Delete" asp-route-id="@item.BallotIssueId">@Localizer["Delete"]</a>
                </td>
            </tr>
        }
    </tbody>
</table>
