@model IEnumerable<Web.Models.PollingPlace>
@using  Microsoft.AspNetCore.Mvc.Localization
@using Microsoft.Extensions.Localization
@using Web
@inject IStringLocalizer<SharedResource> Localizer

@{
    ViewData["Title"] = @Localizer["pollingPlaces_index_title"];
}

<h1>@ViewData["Title"]</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">@Localizer["pollingPlaces_index_create"]</a>
</p>
<table class="table panel panel-default">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PollingPlaceName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PollingStationName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PollingPlaceName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PollingStationName)
            </td>
            <td class="button-group">
                <a class="btn btn-primary btn-sm" asp-action="Edit" asp-route-id="@item.PollingPlaceId">@Localizer["editButton"]</a>
                <a class="btn btn-info btn-sm" asp-action="Details" asp-route-id="@item.PollingPlaceId">@Localizer["detailButton"]</a>
                <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.PollingPlaceId">@Localizer["deleteButton"]</a>
            </td>
        </tr>
        }
    </tbody>
</table>
